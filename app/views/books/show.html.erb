book.show
<p id="notice"><%= notice %></p>
<div class="container px5 px-sm-0">
  <div id="error_explanation">
     <% if @book.errors.any? %>

    <h3><%= @book.errors.count %>errors prohibited this obj from being saved:</h3>
    <% @book.errors.full_messages.each do |message| %>
    <p>・<%= message %></p>
    <% end %>
    <% end %>
    </div>
    <div class="row">
        <div class= "col-md-3">
            <p><%= render 'users/list', user: current_user %></p>
        </div>
        <div class="col-md-8 offset-md-1">
          <h2>Book detail</h2>
          <table>
            <tbody>
              <tr>
                <!--<td>%= link_to user_path(book.id) do %>-->
                <td><%= attachment_image_tag @book_find.user, :profile_image, size:"40x40", fallback: "no_image.jpg" %>
               <br><%= @user.name %></br></td>
                <td><%= @book_find.title %></td>
                <td><%= @book_find.body %></td>
                <!--% if @user.id == current_user.id %>-->
                <!--<td>%= link_to "edit", edit_book_path(@book_find.id) %></td>-->
                <!--<td>%= link_to "Destroy", book_path(@book_find.id), method: :delete, "data-confirm" => "Are you sure?" %></td>-->
                <!--% end %>-->
              </tr>
            </tbody>
          </table>
      </div>
    </div>
  </div>