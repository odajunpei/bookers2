user.show
<p id="notice"><%= notice %></p>
<div class="container px5 px-sm-0">
     <% if @book.errors.any? %>
    <h3><%= @book.errors.count %>errors prohibited this obj from being saved:</h3>
    <% @book.errors.full_messages.each do |message| %>
    <p>・<%= message %></p>
    <% end %>
    <% end %>
    <div class="row">
        <div class= "col-md-3">
            <p><%= render 'users/list', user: @user %></p>
        </div>
        <div class="col-md-8 offset-md-1">
          <h2>Books</h2>
          <table>
            <thread>
              <tr>
                <th></th>
                <th>Title</th>
                <th>Opinion</th>
                <th colspan="3"></th>
              </tr>
            </thread>
            <tbody>
      <% @books.each do |book| %>
              <tr>
                 <td><%= attachment_image_tag book.user, :profile_image, size:"40x40", fallback: "no_image.jpg" %>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
              </tr>
      <% end %>
            </tbody>
          </table>
      </div>
    </div>
  </div>
</div>