  <h3>User info</h3>
   <p><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></p>
   <table class="table">
       <tbody>
           <tr></tr>
           <tr>
               <th>name</th>
               <th class="profile-name"><%= user.name %></th>
            </tr>
            <tr>
               <th>introduction</th>
               <th><%= user.introduction %></th>
            </tr>
        </tbody>
    </table>
  <%= link_to edit_user_path(user), class: 'display:inlinep-block' do %>
  <p><i class="fas fa-user-cog"></i>
  <% end %>
<p><%= render 'books/list', book: @book %></P>
